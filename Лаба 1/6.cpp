#include<iostream>
using namespace std;
#include<conio.h>
#include<math.h>
#include<stdio.h>
using namespace std;
int main()
{
	float S, m, n, a, b, x, z;
	cout<<"S: ";
	cin>>S;
	cout << "m/n: " << (m / n);
	b = sqrt(S * n * m);
	cout<<"b: ";
	cin>>b;
	a = S / b;
	cout << "a: " << a;
	return 0;
}